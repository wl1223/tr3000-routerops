# RouterOps knowledge base

Categories: `device`, `firmware`, `openwrt`, `openclash`, `f50`, and `vps`.

Each durable knowledge item must validate against `schema.json`. Device commands may only
be derived from sources whose applicability matches the discovered model and firmware.
Generic OpenWrt material remains read-only reference until verified on QWRT R26.1.1.

